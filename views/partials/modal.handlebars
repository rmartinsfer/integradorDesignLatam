<div id="myModal" class="modal-trecho">
  <div class="modal-content-trecho">
    <span class="close">&times;</span>
    <div class="control-dados table-responsive">
      <table
        id="example"
        class="table table-striped table-hover table-responsive"
      >
        <thead>
          <tr>
            <th>Código</th>
            <th>Origem</th>
            <th>Destino</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          {{#each trecho}}
            <tr data-id="{{this.idTrecho}}">
              <td>{{this.idTrecho}}</td>
              <td>{{this.origem}}</td>
              <td>{{this.destino}}</td>
              <td class="button"><button
                  type="button"
                  class="search-modal search-button"
                >Selecionar</button></td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>
</div>